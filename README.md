# Unity MCP Plugin Abstractions

Small Unity package containing `UnityMcp.Plugin.Abstractions` contracts for external Unity MCP and Agent Bridge plugins.

This repository is intentionally documentation-light. Requirements, architecture notes, task tracking, and acceptance evidence are maintained in the Unity Agent Bridge workbench.
